import 'package:flutter/material.dart';

class RegisterViewFooter extends StatelessWidget {
  const RegisterViewFooter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}